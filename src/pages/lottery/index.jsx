import Layout from "@/components/Layout";

const Lottory = ({ Component, pageProps }) => {
    return (
        <Layout>
            {/* <Component {...pageProps} /> */}
            <div>Lottory</div>
        </Layout>
    )
}

export default Lottory